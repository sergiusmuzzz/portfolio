extends ./../layouts/master

block css-page

block vars
  - var bodyClass = 'border-box'
  - var pageClass = 'news-list-page'
  - var Title = seed.site.name + ' - ' + seed.site.title
  - var Url = seed.site.url
  - var Section = "News"

block main
  main
    include ./../includes/news-grid